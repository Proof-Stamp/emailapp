name: Deprecated dual verifier freeze workflow

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Verifier artifacts are built from locked source during the normal build."
