{
  "name": "proofstamp-email-receipt",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "check": "npm test && npm run build"
  },
  "engines": {
    "node": "22.x"
  }
}
